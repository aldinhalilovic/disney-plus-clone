# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - master
env:
  REACT_APP_apiKey: ${{ secrets.REACT_APP_API_KEY }}
  REACT_APP_authDomain: ${{ secrets.REACT_APP_AUTH_DOMAIN }}
  REACT_APP_projectId: ${{ secrets.REACT_APP_PROJECT_ID }}
  REACT_APP_storageBucket: ${{ secrets.REACT_APP_STORAGE_BUCKET}}
  REACT_APP_messagingSenderId: ${{ secrets.REACT_APP_MESSAGING_SENDER_ID}}
  REACT_APP_appId: ${{ secrets.REACT_APP_APP_ID }}
  REACT_APP_measurementId: ${{ secrets.REACT_APP_MEASUREMENT_ID }}
  REACT_APP_databaseURL: ${{secrets.REACT_APP_DATABASEURL}}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - run: yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_DISNEY_CLONE_5BAF3 }}"
          channelId: live
          projectId: disney-clone-5baf3
